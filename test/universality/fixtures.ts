import type { FieldDefinition } from "../../src/domain/project-schema.js";
export const SCHEMA_A: readonly FieldDefinition[] = [{ id: "a-17", name: "Phase Alpha", fieldType: "enum[1]", valueType: "enum", valueShape: "entity", cardinality: "single", required: false, writability: "writable", valuesComplete: true, allowedValues: [{ id: "a-next", name: "Next Alpha", kind: "enum" }], provenance: ["admin_project_fields"] }];
export const SCHEMA_B: readonly FieldDefinition[] = [{ id: "z-804", name: "Workflow Marker Z", fieldType: "enum[*]", valueType: "enum", valueShape: "entity", cardinality: "multi", required: true, writability: "writable", valuesComplete: true, allowedValues: [{ id: "z-one", name: "One Z", kind: "enum" }, { id: "z-two", name: "Two Z", kind: "enum" }], provenance: ["admin_project_fields"] }];
